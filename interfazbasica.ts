interface Person {
    firstName: string;
    lastName: string;
}

let person:  Person = {
    firstName: 'Juliana',
    lastName: 'Mereles'
}

console.log(`Persona: ${person.firstName} ${person.lastName}`)